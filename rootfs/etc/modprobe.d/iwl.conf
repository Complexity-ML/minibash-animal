# iwlwifi WiFi workarounds for the disk-root target's Intel card.
# power_scheme=1 : CAM (continuously active) -> disables WiFi power-save so the
#                  card does not sleep through the AP's EAPOL key frames.
# 11n_disable=1  : conservative link mode for Intel cards that fail WPA PTK
#                  install on some AP/firmware combinations.
# Keep bt_coex_active at the firmware/kernel default. The working Altitude
# fallback reports N here; forcing it to 0 changes the EAPOL path on this card.
options iwlwifi 11n_disable=1 power_save=0
options iwlmvm power_scheme=1
