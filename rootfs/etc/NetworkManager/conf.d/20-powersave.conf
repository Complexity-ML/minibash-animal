# Keep WiFi awake. The Intel card's power-save makes it sleep and go silent
# (the box becomes unreachable over WiFi while still "connected") -- fatal for a
# headless box we drive over WiFi. 2 = disable power save for all WiFi devices.
[connection]
wifi.powersave=2
